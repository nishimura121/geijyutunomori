

<header>
  <nav class="navbar navbar-expand-sm navbar-light header-list">
   <% if user_signed_in? %>
    <%= link_to image_tag("logo1.png"), new_user_path %>
    <% else %>

    <%= link_to image_tag("logo1.png"), "/" %>
    <% end %>

  <div class="collapse navbar-collapse">
    <div class="d-flex flex-column ml-auto">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li>
            <%= link_to '投稿する', new_item_path, class: "nav-link" %>
          </li>
          <li>
            <%= link_to 'マイページ', user_path(current_user), class: "nav-link" %>
          </li>
          <li>
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        </ul>
        <% else %>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'ログイン', new_user_session_path, class: "nav-link" %></li>
          <li><%= link_to " 新規登録", '/users/sign_up', class: "nav-link" %></li>
        </ul>
      <% end %>

    </div>
  </div>
  </nav>
</header>

